"""查询流程相关提示词模板

定义LLM 调用所需的提示词模板。
"""

# 1. 意图与目的地提取提示词 (LLM Call 1)
INTENT_DESTINATION_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的旅游目的地（destinations）和用户的意图类型（intents，例如：路线规划、美食推荐、交通指南、景点介绍等）。

【关键提取规则】
1. 目的地提取必须是最大范围的“宏观目的地”（通常是城市、省份或国家级别）。绝对不要提取具体的街道、景点、酒店或商圈名称。
   - 示例1：用户问“成都怎么去春熙路”，目的地只提取“成都”（不要提取春熙路）。
   - 示例2：用户问“三亚亚特兰蒂斯酒店附近有什么吃的”，目的地只提取“三亚”（不要提取酒店名）。
2. 如果用户使用了代词（如"那里"、"这座城市"），请结合历史会话进行指代消解。
3. 只需要提取确定的信息，如果无法确定，对应字段请返回空列表 []。

请严格返回以下 JSON 格式结果（不要包含任何 Markdown 标记或解释）：
{{
    "destinations": ["成都"],
    "intents": ["交通指南"]
}}"""

# 2. 标准化查询重写提示词 (LLM Call 2)
QUERY_REWRITE_TEMPLATE = """
历史会话：
{history_text}

原始问题：{query}
已确认的知识库标准主题：{confirmed_info}

请将用户的原始问题，结合历史会话以及我们系统确认的【标准主题】，重写为一个独立、完整且表意精确的查询语句。
要求：
1. 重写后的问题必须包含标准主题中的目的地和意图。
2. 问题要自然流畅，适合直接去知识库中进行语义检索。

请直接返回重写后的字符串，不要带有任何多余的解释、标点或格式。
"""

# 假设性文档提示词
USER_HYDE_PROMPT_TEMPLATE = """请模拟一段专业旅游攻略或目的地指南中的正文段落，该段落能够完美解答以下用户问题。

相关目的地/主题:
{topic_hint}

用户问题：
{rewritten_query}

要求：

以旅游指南、导游手册或专业游记的客观口吻撰写，使用陈述句。

直接提供旅游干货信息，不要出现"假设"、"您可能需要"、"大概"等不确定或对话式的用语。

包含具体的景点名称、特色美食、交通路线、游玩注意事项等高频旅游关键词，保持回答与查询主题高度相关。

不需要任何标题、寒暄和格式标记，直接输出正文段落。

语言精炼，字数控制在200-300字左右。
"""


# LLM实体名提取提示词模版 (旅游场景)
ENTITY_EXTRACT_SYSTEM_PROMPT = """
你是一个智能旅游向导知识图谱的"实体识别"领域专家。
请从用户问题中抽取用于查询图数据库(Neo4j)的核心旅游实体名称。

【约束】

优先抽取旅游场景下的具体名词短语（例如：具体的景点名称、特色美食、酒店住宿、交通枢纽、游玩项目等）。

注意：请勿提取宽泛的宏观城市或目的地名称（如“成都”、“三亚”、“北京”等），只提取该目的地下的具体微观实体。

每个实体名称不超过 {MAX_ENTITY_NAME_LENGTH} 个字符，超过请截取核心部分，实体名之间不要重复。

绝对不要输出完整句子或多余的解释，只输出实体关键词。

输出必须是严格的 JSON 格式，只含一个字段 entities（字符串数组），不要带有 Markdown 标记。

请严格遵循提示词的要求，如果违背，将导致底层图谱检索系统崩溃。

【输出示例】
{{"entities": ["亚特兰蒂斯酒店", "蜈支洲岛", "清补凉", "春熙路"]}}
"""


ANSWER_PROMPT = """你是一位热情、专业的旅游向导和智能客服，请结合系统提供的参考内容回答用户的问题。

要求：

必须基于【参考内容】和【图谱关系描述】作答，提取最相关的景点、路线、美食、住宿或交通信息，禁止编造不存在的旅游事实或虚假价格。

语言要热情、自然、专业，可以适当使用分点或段落排版以增强用户的阅读体验。

如果用户的问题可以通过图片辅助说明（例如：风景实拍、旅游路线图、地图指南、特色美食等），且【参考内容】中确实包含了相关图片的URL链接，请在答案的最末尾追加一个独立的图片区块，格式严格如下：
【图片】
<图片URL1>
<图片URL2>
（每行仅保留一个URL，不要使用 Markdown 的 ![] 语法包裹；如果没有提取到真实相关的图片链接，则绝对不要输出【图片】区块及其内容）

【参考内容】
{context}

【历史对话】
{history}

【相关目的地与意图】
目的地：{destinations}
查询意图：{intents}

【图谱关系描述】
{graph_relation_description}

【用户问题】
{question}

请给出您的专业回答："""